using System;
using System.Collections.Generic;
using System.Linq;
using TicTacTec.TA.Library;

namespace GipPer
{
    public partial class Indicators
    {
        public SSTO SSTOIndicators { get; set; }
        public MACD MACDIndicators { get; set; }
        public RSI RSIIndicators { get; set; }
        public Acos AcosIndicators { get; set; }
        public AD ADIndicators { get; set; }
        public ADD ADDIndicators { get; set; }
        public Adx IND3Indicators { get; set; }
        public AdOsc AdOscIndicators { get; set; }
        public AdxR AdxRIndicators { get; set; }
        public Apo ApoIndicators { get; set; }
        public Aroon AroonIndicators { get; set; }
        public AroonOsc AroonOscIndicators { get; set; }
        public Asin AsinIndicators { get; set; }
        public Atan AtanIndicators { get; set; }
        public ATR AtrIndicators { get; set; }
        public AveragePrice AveragePriceIndicators { get; set; }
        public BollingerBands BollingerBandsIndicators { get; set; }
        public Beta BetaIndicators { get; set; }
        public Bop BopIndicators { get; set; }
        public Cci CciIndicators { get; set; }
        public Cdl2Crows Cdl2CrowsIndicators { get; set; }
        public Cdl3BlackCrows Cdl3BlackCrowsIndicators { get; set; }
        public Cdl3Inside Cdl3InsideIndicatorsIndicators { get; set; }
        public Cdl3LineStrike Cdl3LineStrikeIndicatorsIndicators { get; set; }
        public Cdl3Outside Cdl3OutsideIndicators { get; set; }
        public Cdl3StarsInSouth Cdl3StarsInSouthIndicators { get; set; }
        public Cdl3WhiteSoldiers Cdl3WhiteSoldiersIndicators { get; set; }
        public CdlAbandonedBaby CdlAbandonedBabyIndicators { get; set; }
        public CdlAdvanceBlock CdlAdvanceBlockIndicators { get; set; }
        public CdlBeltHold CdlBeltHoldIndicators { get; set; }
        public CdlBreakaway CdlBreakawayIndicators { get; set; }
        public CdlClosingMarubozu CdlClosingMarubozuIndicators { get; set; }
        public CdlConcealBabysWall CdlConcealBabysWallIndicators { get; set; }
        public CdlCounterAttack CdlCounterAttackIndicators { get; set; }
        public CdlDarkCloudCover CdlDarkCloudCoverIndicators { get; set; }
        public CdlDoji CdlDojiIndicators { get; set; }
        public CdlDojiStar CdlDojiStarIndicators { get; set; }
        public CdlDragonflyDoji CdlDragonflyDojiIndicators { get; set; }
        public CdlEngulfing CdlEngulfingIndicators { get; set; }
        public CdlEveningDojiStar CdlEveningDojiStarIndicators { get; set; }
        public CdlEveningStar CdlEveningStarIndicators { get; set; }
        public CdlGapSideSideWhite CdlGapSideSideWhiteIndicators { get; set; }
        public CdlGravestoneDoji CdlGravestoneDojiIndicators { get; set; }
        public CdlHammer CdlHammerIndicators { get; set; }
        public CdlHangingMan CdlHangingManIndicators { get; set; }
        public CdlHarami CdlHaramiIndicators { get; set; }
        public CdlHaramiCross CdlHaramiCrossIndicators { get; set; }
        public CdlHignWave CdlHignWaveIndicators { get; set; }
        public CdlHikkake CdlHikkakeIndicators { get; set; }
        public CdlHikkakeMod CdlHikkakeModIndicators { get; set; }
        public CdlHomingPigeon CdlHomingPigeonIndicators { get; set; }
        public CdlIdentical3Crows CdlIdentical3CrowsIndicators { get; set; }
        public CdlInNeck CdlInNeckIndicators { get; set; }
        public CdlInvertedHammer CdlInvertedHammerIndicators { get; set; }
        public CdlKicking CdlKickingIndicators { get; set; }
        public CdlLadderBottom CdlLadderBottomIndicators { get; set; }
        public CdlKickingByLength CdlKickingByLengthIndicators { get; set; }
        public CdlLongLeggedDoji CdlLongLeggedDojiIndicators { get; set; }
        public CdlLongLine CdlLongLineIndicators { get; set; }
        public CdlMarubozu CdlMarubozuIndicators { get; set; }
        public CdlMatHold CdlMatHoldIndicators { get; set; }
        public CdlMatchingLow CdlMatchingLowIndicators { get; set; }
        public CdlMorningDojiStar CdlMorningDojiStarIndicators { get; set; }
        public CdlMorningStar CdlMorningStarIndicators { get; set; }
        public CdlOnNeck CdlOnNeckIndicators { get; set; }
        public CdlPiercing CdlPiercingIndicators { get; set; }
        public CdlRickshawMan CdlRickshawManIndicators { get; set; }
        public CdlRiseFall3Methods CdlRiseFall3MethodsIndicators { get; set; }
        public CdlSeperatingLines CdlSeperatingLinesIndicators { get; set; }
        public CdlShootingStar CdlShootingStarIndicators { get; set; }
        public CdlShortLine CdlShortLineIndicators { get; set; }
        public CdlSpinningTop CdlSpinningTopIndicators { get; set; }
        public CdlStalledPattern CdlStalledPatternIndicators { get; set; }
        public CdlStickSandwhich CdlStickSandwhichIndicators { get; set; }
        public CdlTakuri CdlTakuriIndicators { get; set; }
        public CdlTasukiGap CdlTasukiGapIndicators { get; set; }
        public CdlThrusting CdlThrustingIndicators { get; set; }
        public CdlTristar CdlTristarIndicators { get; set; }
        public CdlUnique3River CdlUnique3RiverIndicators { get; set; }
        public CdlUpsideGap2Crows CdlUpsideGap2CrowsIndicators { get; set; }
        public CdlXSideGap3Methods CdlXSideGap3MethodsIndicators { get; set; }
        public Ceil CeilIndicators { get; set; }
        public Cmo CmoIndicators { get; set; }
        public Cos CosIndicators { get; set; }
        public Cosh CoshIndicators { get; set; }
        public Dema DemaIndicators { get; set; }
        public Div DivIndicators { get; set; }
        public Dx DxIndicators { get; set; }
        public Ema EmaIndicators { get; set; }
        public Exp ExpIndicators { get; set; }
        public Floor FloorIndicators { get; set; }
        public HtDcPeriod HtDcPeriodIndicators { get; set; }
        public HtDcPhase HtDcPhaseIndicators { get; set; }
        public HtPhasor HtPhasorIndicators { get; set; }
        public HtSine HtSineIndicators { get; set; }
        public HtTrendline HtTrendlineIndicators { get; set; }
        public HtTrendMode HtTrendModeIndicators { get; set; }
        public Kama KamaIndicators { get; set; }
        public LinearReg LinearRegIndicators { get; set; }
        public LinearRegAngle LinearRegAngleIndicators { get; set; }
        public LinearRegIntercept LinearRegInterceptIndicators { get; set; }
        public LinearRegSlope LinearRegSlopeIndicators { get; set; }
        public Ln LnIndicators { get; set; }
        public Log10 Log10Indicators { get; set; }
        public MacdExt MacdExtIndicators { get; set; }
        public MacdFix MacdFixIndicators { get; set; }
        public Mama MamaIndicators { get; set; }
        public Max MaxIndicators { get; set; }
        public MaxIndex MaxIndexIndicators { get; set; }
        public MedPrice MedPriceIndicators { get; set; }
        public Mfi MfiIndicators { get; set; }
        public MidPoint MidPointIndicators { get; set; }
        public MidPrice MidPriceIndicators { get; set; }
        public Min MinIndicators { get; set; }
        public MinIndex MinIndexIndicators { get; set; }
        public MinMax MinMaxIndicators { get; set; }
        public MinusDI MinusDIIndicators { get; set; }
        public MinusDM MinusDMIndicators { get; set; }
        public Mom MomIndicators { get; set; }
        public MovingAverage MovingAverageIndicators { get; set; }
        public MovingAverageVariablePeriod MovingAverageVariablePeriodIndicators { get; set; }
        public Mult MultIndicators { get; set; }
        public Natr NatrIndicators { get; set; }
        public Obv ObvIndicators { get; set; }
        public PlusDI PlusDIIndicators { get; set; }
        public PlusDM PlusDMIndicators { get; set; }
        public Ppo PpoIndicators { get; set; }
        public Roc RocIndicators { get; set; }
        public RocP RocPIndicators { get; set; }
        public RocR RocRIndicators { get; set; }
        public RocR100 RocR100Indicators { get; set; }
        public Sar SarIndicators { get; set; }
        public SarExt SarExtIndicators { get; set; }
        public Sin SinIndicators { get; set; }
        public Sinh SinhIndicators { get; set; }
        public Sma SmaIndicators { get; set; }
        public Sqrt SqrtIndicators { get; set; }
        public StdDev StdDevIndicators { get; set; }
        public StochF StochFIndicators { get; set; }
        public StochRsi StochRsiIndicators { get; set; }
        public Sub SubIndicators { get; set; }
        public Sum SumIndicators { get; set; }
        public T3 T3Indicators { get; set; }
        public Tan TanIndicators { get; set; }
        public Tanh TanhIndicators { get; set; }
        public Tema TemaIndicators { get; set; }
        public Trima TrimaIndicators { get; set; }
        public Trix TrixIndicators { get; set; }
        public TrueRange TrueRangeIndicators { get; set; }
        public Tsf TsfIndicators { get; set; }
        public TypPrice TypPriceIndicators { get; set; }
        public UltOsc UltOscIndicators { get; set; }
        public WclPrice WclPriceIndicators { get; set; }
        public Variance VarianceIndicators { get; set; }
        public WillR WillRIndicators { get; set; }
        public Wma WmaIndicators { get; set; }


        public List<KeyValuePair<int, double>> GetIntoChartFormat(List<double> input)

        {
            double[] Shift = new double[input.Count];
            int count = 0;
            int otherCount = input.Count - 1;
            for (int i = input.Count - 1; i >= 0; i--)
            {
                if (input[i] != 0)
                {
                    Shift[otherCount] = input[i];
                    otherCount--;
                }
                else count++;
            }

            List<KeyValuePair<int, double>> V = new List<KeyValuePair<int, double>>();
            for (int i = 0; i < Shift.Length; i++)
            {
                V.Add(new KeyValuePair<int, double>(i, Shift[i]));
            }

            for(int i = V.Count - 1; i>= 0; i--)
            {
                if (V[i].Value == 0) V.RemoveAt(i);
            }
            return V;
        }

        public class MACD
        {
            public List<double> Fast { get; set; }
            public List<double> Signal { get; set; }
            public List<double> Slow { get; set; }

            public MACD(int fast, int slow, int signal, int recent, int early, List<double> real)
            {
                Fast = new List<double>();
                Signal = new List<double>();
                Slow = new List<double>();

                double[] Real = real.ToArray();
                int optInFastPeriod = fast;
                int SlowPeriod = slow;
                int optInSignalPeriod = signal;
                int endindex = recent + early;

                int begidx;
                int NBElement;
                double[] MACD = new double[endindex];
                double[] MACSignal = new double[endindex];
                double[] MACDHist = new double[endindex];

                TicTacTec.TA.Library.Core.Macd(recent, early, Real, optInFastPeriod, SlowPeriod, optInSignalPeriod,
                    out begidx, out NBElement, MACD, MACSignal, MACDHist);
                Fast = MACD.ToList();
                Signal = MACSignal.ToList();
                Slow = MACDHist.ToList();

            }

        }

        public class SSTO
        {
            public List<double> SSTOK { get; set; }
            public List<double> SSTOD { get; set; }

            public SSTO(int fastk, int k, int d, int recent, int early, List<double> highs, List<double> lows,
                List<double> close, Core.MAType KMaType, Core.MAType DMaType)
            {
                SSTOK = new List<double>();
                SSTOD = new List<double>();

                double[] Highs = highs.ToArray();
                double[] Lows = lows.ToArray();
                double[] Close = close.ToArray();

                int begidx;
                int NBElement;
                double[] slowk = new double[early - recent];
                double[] slowd = new double[early - recent];
                Core.RetCode a = TicTacTec.TA.Library.Core.Stoch(recent, early, Highs, Lows, Close, fastk, k, KMaType, d,
                    DMaType, out begidx, out NBElement, slowk, slowd);

                SSTOK = slowk.ToList();
                SSTOD = slowd.ToList();
            }
        }

        public class RSI
        {
            public List<double> RSIHist { get; set; }

            public RSI(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                RSIHist = new List<double>();

                int begidx;
                int NBElement;
                double[] Real = new double[Math.Abs(early - recent)];
                TicTacTec.TA.Library.Core.Rsi(recent, early, inReal, optInTimePeriod, out begidx, out NBElement, Real);

                RSIHist = Real.ToList();
            }
        }

        public class Acos
        {
            public List<double> AcosHist { get; set; }

            public Acos(int recent, int early, double[] inReal)
            {
                AcosHist = new List<double>();

                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Acos(recent, early, inReal, out begidx, out NBElement, Returned);
                AcosHist = Returned.ToList();

            }
        }

        public class AD
        {
            public List<double> ADHist { get; set; }

            public AD(int recent, int early, double[] highs, double[] lows, double[] closes, double[] volumes)
            {
                ADHist = new List<double>();

                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Ad(recent, early, highs, lows, closes, volumes, out begidx, out NBElement,
                    Returned);
                ADHist = Returned.ToList();
            }
        }

        public class ADD
        {
            public List<double> ADDHist { get; set; }

            public ADD(int recent, int early, double[] inReal0, double[] inReal1)
            {
                ADDHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Add(recent, early, inReal0, inReal1, out begidx, out NBElement, Returned);
                ADDHist = Returned.ToList();
            }
        }

        public class AdOsc
        {
            public List<double> AdOscHist { get; set; }

            public AdOsc(int recent, int early, double[] Highs, double[] Lows, double[] Closes, double[] Volumes,
                int optInFastPeriod, int SlowPeroid)
            {
                AdOscHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.AdOsc(recent, early, Highs, Lows, Closes, Volumes, optInFastPeriod, SlowPeroid,
                    out begidx, out NBElement, Returned);
                AdOscHist = Returned.ToList();
            }
        }

        public class Adx
        {
            public List<double> AdxHist { get; set; }

            public Adx(int recent, int early, double[] HIghs, double[] Lows, double[] Closes, int optInTimePeriod)
            {
                AdxHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Adx(recent, early, HIghs, Lows, Closes, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                AdxHist = Returned.ToList();
            }
        }

        public class AdxR
        {
            public List<double> AdxRHist { get; set; }

            public AdxR(int recent, int early, double[] HIghs, double[] Lows, double[] Closes, int optInTimePeriod)
            {
                AdxRHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Adxr(recent, early, HIghs, Lows, Closes, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                AdxRHist = Returned.ToList();
            }
        }

        public class Apo
        {
            public List<double> ApoHist { get; set; }

            public Apo(int recent, int early, double[] inReal, int optInFastPeriod, int optInSlowPeriod,
                Core.MAType optInMATYPE)
            {
                ApoHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Apo(recent, early, inReal, optInFastPeriod, optInSlowPeriod, optInMATYPE,
                    out begidx, out NBElement, Returned);
                ApoHist = Returned.ToList();
            }
        }

        public class Aroon
        {
            public List<double> AroonUp { get; set; }
            public List<double> AroonDown { get; set; }

            public Aroon(int recent, int early, double[] highs, double[] lows, int optInTimePeriod)
            {
                AroonDown = new List<double>();
                AroonUp = new List<double>();
                int begidx;
                int NBElement;
                double[] aroonUp = new double[early - recent];
                double[] aroonDown = new double[early - recent];
                TicTacTec.TA.Library.Core.Aroon(recent, early, highs, lows, optInTimePeriod, out begidx, out NBElement,
                    aroonDown, aroonUp);
                AroonUp = aroonUp.ToList();
                AroonDown = aroonDown.ToList();
            }
        }

        public class AroonOsc
        {
            public List<double> AroonOscHist { get; set; }

            public AroonOsc(int recent, int early, double[] inHigh, double[] inLow, int Timeperiod)
            {
                AroonOscHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.AroonOsc(recent, early, inHigh, inLow, Timeperiod, out begidx, out NBElement,
                    Returned);
                AroonOscHist = Returned.ToList();
            }
        }

        public class Asin
        {
            public List<double> AsinHist { get; set; }

            public Asin(int recent, int early, double[] inReal)
            {
                AsinHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Asin(recent, early, inReal, out begidx, out NBElement, Returned);
                AsinHist = Returned.ToList();
            }
        }

        public class Atan
        {
            public List<double> AtanHIst { get; set; }

            public Atan(int recent, int early, double[] inReal)
            {
                AtanHIst = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Atan(recent, early, inReal, out begidx, out NBElement, Returned);
                AtanHIst = Returned.ToList();
            }
        }

        public class ATR
        {
            public List<double> ATRHist { get; set; }

            public ATR(int recent, int early, double[] Highs, double[] Lows, double[] inClose, int timeperiod)
            {
                ATRHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Atr(recent, early, Highs, Lows, inClose, timeperiod, out begidx, out NBElement,
                    Returned);
                ATRHist = Returned.ToList();
            }
        }

        public class AveragePrice
        {
            public List<double> AveragePriceHist { get; set; }

            public AveragePrice(int recent, int early, double[] Opens, double[] Highs, double[] Lows, double[] inClose)
            {
                AveragePriceHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.AvgPrice(recent, early, Opens, Highs, Lows, inClose, out begidx, out NBElement,
                    Returned);
                AveragePriceHist = Returned.ToList();
            }
        }

        public class BollingerBands
        {
            public List<double> RealUpperHist { get; set; }
            public List<double> RealMidHist { get; set; }
            public List<double> RealLowerHist { get; set; }

            public BollingerBands(int recent, int early, double[] inReal, int OptinTimePeriod, int optInNbDevUp,
                int optInNbDevDn, Core.MAType OptInMATYPE)
            {
                RealUpperHist = new List<double>();
                RealMidHist = new List<double>();
                RealLowerHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Upper = new double[early - recent];
                double[] Mid = new double[early - recent];
                double[] Lower = new double[early - recent];
                TicTacTec.TA.Library.Core.Bbands(recent, early, inReal, OptinTimePeriod, optInNbDevUp, optInNbDevDn,
                    OptInMATYPE, out begidx, out NBElement, Upper, Mid, Lower);
                RealUpperHist = Upper.ToList();
                RealMidHist = Mid.ToList();
                RealLowerHist = Lower.ToList();

            }
        }

        public class Beta
        {
            public List<double> BetaHist { get; set; }

            public Beta(int recent, int early, double[] inReal0, double[] inReal1, int optInTimePeriod)
            {
                BetaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Beta(recent, early, inReal0, inReal1, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                BetaHist = Returned.ToList();
            }
        }

        public class Bop
        {
            public List<double> BopHist { get; set; }

            public Bop(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                BopHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Bop(recent, early, inOpen, inHigh, inLow, inClose, out begidx, out NBElement,
                    Returned);
                BopHist = Returned.ToList();
            }
        }

        public class Cci
        {
            public List<double> CciHist { get; set; }

            public Cci(int recent, int early, double[] inHigh, double[] inLow, double[] inClose, int optInTimePeriod)
            {
                CciHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Cci(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                CciHist = Returned.ToList();
            }
        }

        public class Cdl2Crows
        {
            public List<int> Cdl2CrowsHist { get; set; }

            public Cdl2Crows(int recent, int early, double[] inHigh, double[] inLow, double[] inClose, double[] inOpen)
            {
                Cdl2CrowsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl2Crows(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl2CrowsHist = Returned.ToList();
            }
        }

        public class Cdl3BlackCrows
        {
            public List<int> Cdl3BlackCrowsHist { get; set; }

            public Cdl3BlackCrows(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                Cdl3BlackCrowsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3BlackCrows(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3BlackCrowsHist = Returned.ToList();
            }
        }

        public class Cdl3Inside
        {
            public List<int> Cdl3InsideHist { get; set; }

            public Cdl3Inside(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                Cdl3InsideHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3Inside(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3InsideHist = Returned.ToList();
            }
        }

        public class Cdl3LineStrike
        {
            public List<int> Cdl3LineStrikeHist { get; set; }

            public Cdl3LineStrike(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                Cdl3LineStrikeHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3LineStrike(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3LineStrikeHist = Returned.ToList();
            }
        }

        public class Cdl3Outside
        {
            public List<int> Cdl3OutsideHist { get; set; }

            public Cdl3Outside(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                Cdl3OutsideHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3Outside(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3OutsideHist = Returned.ToList();
            }
        }

        public class Cdl3StarsInSouth
        {
            public List<int> Cdl3StarsInSouthHist { get; set; }

            public Cdl3StarsInSouth(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                Cdl3StarsInSouthHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3StarsInSouth(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3StarsInSouthHist = Returned.ToList();
            }
        }

        public class Cdl3WhiteSoldiers
        {
            public List<int> Cdl3WhiteSoldiersHist { get; set; }

            public Cdl3WhiteSoldiers(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                Cdl3WhiteSoldiersHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.Cdl3WhiteSoldiers(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                Cdl3WhiteSoldiersHist = Returned.ToList();
            }
        }

        public class CdlAbandonedBaby
        {
            public List<int> CdlAbandonedBabyHist { get; set; }

            public CdlAbandonedBaby(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, int optInPenetration)
            {
                CdlAbandonedBabyHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlAbandonedBaby(recent, early, inOpen, inHigh, inLow, inClose,
                    optInPenetration, out begidx, out NBElement, Returned);
                CdlAbandonedBabyHist = Returned.ToList();
            }
        }

        public class CdlAdvanceBlock
        {
            public List<int> CdlAdvanceBlockHist { get; set; }

            public CdlAdvanceBlock(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlAdvanceBlockHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlAdvanceBlock(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlAdvanceBlockHist = Returned.ToList();
            }
        }

        public class CdlBeltHold
        {
            public List<int> CdlBeltHoldHist { get; set; }

            public CdlBeltHold(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlBeltHoldHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlBeltHold(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlBeltHoldHist = Returned.ToList();
            }
        }

        public class CdlBreakaway
        {
            public List<int> CdlBreakawayHist { get; set; }

            public CdlBreakaway(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlBreakawayHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlBreakaway(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlBreakawayHist = Returned.ToList();
            }
        }

        public class CdlClosingMarubozu
        {
            public List<int> CdlClosingMarubozuHist { get; set; }

            public CdlClosingMarubozu(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlClosingMarubozuHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlClosingMarubozu(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlClosingMarubozuHist = Returned.ToList();
            }
        }

        public class CdlConcealBabysWall
        {
            public List<int> CdlConcealBabysWallHist { get; set; }

            public CdlConcealBabysWall(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlConcealBabysWallHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlConcealBabysWall(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlConcealBabysWallHist = Returned.ToList();
            }
        }

        public class CdlCounterAttack
        {
            public List<int> CdlCounterAttackHist { get; set; }

            public CdlCounterAttack(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlCounterAttackHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlCounterAttack(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlCounterAttackHist = Returned.ToList();
            }
        }

        public class CdlDarkCloudCover
        {
            public List<int> CdlDarkCloudCoverHist { get; set; }

            public CdlDarkCloudCover(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, int optInPenetration)
            {
                CdlDarkCloudCoverHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlDarkCloudCover(recent, early, inOpen, inHigh, inLow, inClose,
                    optInPenetration, out begidx, out NBElement, Returned);
                CdlDarkCloudCoverHist = Returned.ToList();
            }
        }

        public class CdlDoji
        {
            public List<int> CdlDojiHist { get; set; }

            public CdlDoji(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlDojiHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlDoji(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlDojiHist = Returned.ToList();
            }
        }

        public class CdlDojiStar
        {
            public List<int> CdlDojiStarHist { get; set; }

            public CdlDojiStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlDojiStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlDojiStar(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlDojiStarHist = Returned.ToList();
            }
        }

        public class CdlDragonflyDoji
        {
            public List<int> CdlDragonflyDojiHist { get; set; }

            public CdlDragonflyDoji(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlDragonflyDojiHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlDragonflyDoji(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlDragonflyDojiHist = Returned.ToList();
            }
        }

        public class CdlEngulfing
        {
            public List<int> CdlEngulfingHist { get; set; }

            public CdlEngulfing(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlEngulfingHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlEngulfing(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlEngulfingHist = Returned.ToList();
            }
        }

        public class CdlEveningDojiStar
        {
            public List<int> CdlEveningDojiStarHist { get; set; }

            public CdlEveningDojiStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, double optInPenetration)
            {
                CdlEveningDojiStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlEveningDojiStar(recent, early, inOpen, inHigh, inLow, inClose,
                    optInPenetration, out begidx, out NBElement, Returned);
                CdlEveningDojiStarHist = Returned.ToList();
            }
        }

        public class CdlEveningStar
        {
            public List<int> CdlEveningStarHist { get; set; }

            public CdlEveningStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, double optInPenetration)
            {
                CdlEveningStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlEveningStar(recent, early, inOpen, inHigh, inLow, inClose, optInPenetration,
                    out begidx, out NBElement, Returned);
                CdlEveningStarHist = Returned.ToList();
            }
        }

        public class CdlGapSideSideWhite
        {
            public List<int> CdlGapSideSideWhiteHist { get; set; }

            public CdlGapSideSideWhite(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlGapSideSideWhiteHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlGapSideSideWhite(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlGapSideSideWhiteHist = Returned.ToList();
            }
        }

        public class CdlGravestoneDoji
        {
            public List<int> CdlGravestoneDojiHist { get; set; }

            public CdlGravestoneDoji(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlGravestoneDojiHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlGravestoneDoji(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlGravestoneDojiHist = Returned.ToList();
            }
        }

        public class CdlHammer
        {
            public List<int> CdlHammerHist { get; set; }

            public CdlHammer(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlHammerHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHammer(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHammerHist = Returned.ToList();
            }
        }

        public class CdlHangingMan
        {
            public List<int> CdlHangingManHist { get; set; }

            public CdlHangingMan(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlHangingManHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHangingMan(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHangingManHist = Returned.ToList();
            }
        }

        public class CdlHarami
        {
            public List<int> CdlHaramiHist { get; set; }

            public CdlHarami(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlHaramiHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHarami(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHaramiHist = Returned.ToList();
            }
        }

        public class CdlHaramiCross
        {
            public List<int> CdlHaramiCrossHist { get; set; }

            public CdlHaramiCross(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlHaramiCrossHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHaramiCross(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHaramiCrossHist = Returned.ToList();
            }
        }

        public class CdlHignWave
        {
            public List<int> CdlHignWaveHist { get; set; }

            public CdlHignWave(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlHignWaveHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHignWave(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHignWaveHist = Returned.ToList();
            }
        }

        public class CdlHikkake
        {
            public List<int> CdlHikkakeHist { get; set; }

            public CdlHikkake(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlHikkakeHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHikkake(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHikkakeHist = Returned.ToList();
            }
        }

        public class CdlHikkakeMod
        {
            public List<int> CdlHikkakeModHist { get; set; }

            public CdlHikkakeMod(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlHikkakeModHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHikkakeMod(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHikkakeModHist = Returned.ToList();
            }
        }

        public class CdlHomingPigeon
        {
            public List<int> CdlHomingPigeonHist { get; set; }

            public CdlHomingPigeon(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlHomingPigeonHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlHomingPigeon(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlHomingPigeonHist = Returned.ToList();
            }
        }

        public class CdlIdentical3Crows
        {
            public List<int> CdlIdentical3CrowsHist { get; set; }

            public CdlIdentical3Crows(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlIdentical3CrowsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlIdentical3Crows(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlIdentical3CrowsHist = Returned.ToList();
            }
        }

        public class CdlInNeck
        {
            public List<int> CdlInNeckHist { get; set; }

            public CdlInNeck(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlInNeckHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlInNeck(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlInNeckHist = Returned.ToList();
            }
        }

        public class CdlInvertedHammer
        {
            public List<int> CdlInvertedHammerHist { get; set; }

            public CdlInvertedHammer(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlInvertedHammerHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlInvertedHammer(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlInvertedHammerHist = Returned.ToList();
            }
        }

        public class CdlKicking
        {
            public List<int> CdlKickingHist { get; set; }

            public CdlKicking(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlKickingHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlKicking(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlKickingHist = Returned.ToList();
            }
        }

        public class CdlLadderBottom
        {
            public List<int> CdlLadderBottomHist { get; set; }

            public CdlLadderBottom(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlLadderBottomHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlLadderBottom(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlLadderBottomHist = Returned.ToList();
            }
        }

        public class CdlKickingByLength
        {
            public List<int> CdlKickingByLengthHist { get; set; }

            public CdlKickingByLength(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlKickingByLengthHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlKickingByLength(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlKickingByLengthHist = Returned.ToList();
            }
        }

        public class CdlLongLeggedDoji
        {
            public List<int> CdlLongLeggedDojiHist { get; set; }

            public CdlLongLeggedDoji(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlLongLeggedDojiHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlLongLeggedDoji(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlLongLeggedDojiHist = Returned.ToList();
            }
        }

        public class CdlLongLine
        {
            public List<int> CdlLongLineHist { get; set; }

            public CdlLongLine(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlLongLineHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlLongLine(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlLongLineHist = Returned.ToList();
            }
        }

        public class CdlMarubozu
        {
            public List<int> CdlMarubozuHist { get; set; }

            public CdlMarubozu(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlMarubozuHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlMarubozu(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlMarubozuHist = Returned.ToList();
            }
        }

        public class CdlMatHold
        {
            public List<int> CdlMatHoldHist { get; set; }

            public CdlMatHold(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose,
                int optInPenetration)
            {
                CdlMatHoldHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlMatHold(recent, early, inOpen, inHigh, inLow, inClose, optInPenetration,
                    out begidx, out NBElement, Returned);
                CdlMatHoldHist = Returned.ToList();
            }
        }

        public class CdlMatchingLow
        {
            public List<int> CdlMatchingLowHist { get; set; }

            public CdlMatchingLow(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlMatchingLowHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlMatchingLow(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlMatchingLowHist = Returned.ToList();
            }
        }

        public class CdlMorningDojiStar
        {
            public List<int> CdlMorningDojiStarHist { get; set; }

            public CdlMorningDojiStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, double optInPenetration)
            {
                CdlMorningDojiStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlMorningDojiStar(recent, early, inOpen, inHigh, inLow, inClose,
                    optInPenetration, out begidx, out NBElement, Returned);
                CdlMorningDojiStarHist = Returned.ToList();
            }
        }

        public class CdlMorningStar
        {
            public List<int> CdlMorningStarHist { get; set; }

            public CdlMorningStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose, double optInPenetration)
            {
                CdlMorningStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlMorningStar(recent, early, inOpen, inHigh, inLow, inClose, optInPenetration,
                    out begidx, out NBElement, Returned);
                CdlMorningStarHist = Returned.ToList();
            }
        }

        public class CdlOnNeck
        {
            public List<int> CdlOnNeckHist { get; set; }

            public CdlOnNeck(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlOnNeckHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlOnNeck(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlOnNeckHist = Returned.ToList();
            }
        }

        public class CdlPiercing
        {
            public List<int> CdlPiercingHist { get; set; }

            public CdlPiercing(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlPiercingHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlPiercing(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlPiercingHist = Returned.ToList();
            }
        }

        public class CdlRickshawMan
        {
            public List<int> CdlRickshawManHist { get; set; }

            public CdlRickshawMan(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlRickshawManHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlRickshawMan(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlRickshawManHist = Returned.ToList();
            }
        }

        public class CdlRiseFall3Methods
        {
            public List<int> CdlRiseFall3MethodsHist { get; set; }

            public CdlRiseFall3Methods(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlRiseFall3MethodsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlRiseFall3Methods(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlRiseFall3MethodsHist = Returned.ToList();
            }
        }

        public class CdlSeperatingLines
        {
            public List<int> CdlSeperatingLinesHist { get; set; }

            public CdlSeperatingLines(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlSeperatingLinesHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlSeperatingLines(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlSeperatingLinesHist = Returned.ToList();
            }
        }

        public class CdlShootingStar
        {
            public List<int> CdlShootingStarHist { get; set; }

            public CdlShootingStar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlShootingStarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlShootingStar(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlShootingStarHist = Returned.ToList();
            }
        }

        public class CdlShortLine
        {
            public List<int> CdlShortLineHist { get; set; }

            public CdlShortLine(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlShortLineHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlShortLine(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlShortLineHist = Returned.ToList();
            }
        }

        public class CdlSpinningTop
        {
            public List<int> CdlSpinningTopHist { get; set; }

            public CdlSpinningTop(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlSpinningTopHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlSpinningTop(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlSpinningTopHist = Returned.ToList();
            }
        }

        public class CdlStalledPattern
        {
            public List<int> CdlStalledPatternHist { get; set; }

            public CdlStalledPattern(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlStalledPatternHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlStalledPattern(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlStalledPatternHist = Returned.ToList();
            }
        }

        public class CdlStickSandwhich
        {
            public List<int> CdlStickSandwhichHist { get; set; }

            public CdlStickSandwhich(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlStickSandwhichHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlStickSandwhich(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlStickSandwhichHist = Returned.ToList();
            }
        }

        public class CdlTakuri
        {
            public List<int> CdlTakuriHist { get; set; }

            public CdlTakuri(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlTakuriHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlTakuri(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlTakuriHist = Returned.ToList();
            }
        }

        public class CdlTasukiGap
        {
            public List<int> CdlTasukiGapHist { get; set; }

            public CdlTasukiGap(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlTasukiGapHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlTasukiGap(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlTasukiGapHist = Returned.ToList();
            }
        }

        public class CdlThrusting
        {
            public List<int> CdlThrustingHist { get; set; }

            public CdlThrusting(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlThrustingHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlThrusting(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlThrustingHist = Returned.ToList();
            }
        }

        public class CdlTristar
        {
            public List<int> CdlTristarHist { get; set; }

            public CdlTristar(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose)
            {
                CdlTristarHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlTristar(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlTristarHist = Returned.ToList();
            }
        }

        public class CdlUnique3River
        {
            public List<int> CdlUnique3RiverHist { get; set; }

            public CdlUnique3River(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlUnique3RiverHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlUnique3River(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlUnique3RiverHist = Returned.ToList();
            }
        }

        public class CdlUpsideGap2Crows
        {
            public List<int> CdlUpsideGap2CrowsHist { get; set; }

            public CdlUpsideGap2Crows(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlUpsideGap2CrowsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlUpsideGap2Crows(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlUpsideGap2CrowsHist = Returned.ToList();
            }
        }

        public class CdlXSideGap3Methods
        {
            public List<int> CdlXSideGap3MethodsHist { get; set; }

            public CdlXSideGap3Methods(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow,
                double[] inClose)
            {
                CdlXSideGap3MethodsHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.CdlXSideGap3Methods(recent, early, inOpen, inHigh, inLow, inClose, out begidx,
                    out NBElement, Returned);
                CdlXSideGap3MethodsHist = Returned.ToList();
            }
        }

        public class Ceil
        {
            public List<double> CeilHist { get; set; }

            public Ceil(int recent, int early, double[] inReal)
            {
                CeilHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Ceil(recent, early, inReal, out begidx, out NBElement, Returned);
                CeilHist = Returned.ToList();
            }
        }

        public class Cmo
        {
            public List<double> CmoHist { get; set; }

            public Cmo(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                CmoHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Cmo(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                CmoHist = Returned.ToList();
            }
        }

        public class Correl
        {
            public List<double> CorrelHist { get; set; }

            public Correl(int recent, int early, double[] inReal0, double[] inReal1, int optInTimePeriod)
            {
                CorrelHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Correl(recent, early, inReal0, inReal1, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                CorrelHist = Returned.ToList();
            }
        }

        public class Cos
        {
            public List<double> CosHist { get; set; }

            public Cos(int recent, int early, double[] inReal)
            {
                CosHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Cos(recent, early, inReal, out begidx, out NBElement, Returned);
                CosHist = Returned.ToList();
            }
        }

        public class Cosh
        {
            public List<double> CoshHist { get; set; }

            public Cosh(int recent, int early, double[] inReal)
            {
                CoshHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Cosh(recent, early, inReal, out begidx, out NBElement, Returned);
                CoshHist = Returned.ToList();
            }
        }

        public class Dema
        {
            public List<double> DemaHist { get; set; }

            public Dema(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                DemaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Dema(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                DemaHist = Returned.ToList();
            }
        }

        public class Div
        {
            public List<double> DivHist { get; set; }

            public Div(int recent, int early, double[] inReal0, double[] inReal1)
            {
                DivHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Div(recent, early, inReal0, inReal1, out begidx, out NBElement, Returned);
                DivHist = Returned.ToList();
            }
        }

        public class Dx
        {
            public List<double> DxHist { get; set; }

            public Dx(int recent, int early, double[] inHigh, double[] inLow, double[] inClose, int optInTimePeriod)
            {
                DxHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Dx(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                DxHist = Returned.ToList();
            }
        }

        public class Ema
        {
            public List<double> EmaHist { get; set; }

            public Ema(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                EmaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[Math.Abs(early - recent)+1];
                TicTacTec.TA.Library.Core.Ema(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                EmaHist = Returned.ToList();
               
            }
        }

        public class Exp
        {
            public List<double> ExpHiist { get; set; }

            public Exp(int recent, int early, double[] inReal)
            {
                ExpHiist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Exp(recent, early, inReal, out begidx, out NBElement, Returned);
                ExpHiist = Returned.ToList();
            }
        }

        public class Floor
        {
            public List<double> FloorHist { get; set; }

            public Floor(int recent, int early, double[] inReal)
            {
                FloorHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Floor(recent, early, inReal, out begidx, out NBElement, Returned);
                FloorHist = Returned.ToList();
            }
        }

        public class HtDcPeriod
        {
            public List<double> HtDcPeriodHist { get; set; }

            public HtDcPeriod(int recent, int early, double[] inReal)
            {
                HtDcPeriodHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.HtDcPeriod(recent, early, inReal, out begidx, out NBElement, Returned);
                HtDcPeriodHist = Returned.ToList();
            }
        }

        public class HtDcPhase
        {
            public List<double> HtDcPhaseHist { get; set; }

            public HtDcPhase(int recent, int early, double[] inReal)
            {
                HtDcPhaseHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.HtDcPhase(recent, early, inReal, out begidx, out NBElement, Returned);
                HtDcPhaseHist = Returned.ToList();
            }
        }

        public class HtPhasor
        {
            public List<double> InPhaseHist { get; set; }
            public List<double> QuadratureHist { get; set; }

            public HtPhasor(int recent, int early, double[] inReal)
            {
                InPhaseHist = new List<double>();
                QuadratureHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                double[] Returned2 = new double[early - recent];
                TicTacTec.TA.Library.Core.HtPhasor(recent, early, inReal, out begidx, out NBElement, Returned, Returned2);
                InPhaseHist = Returned.ToList();
                QuadratureHist = Returned2.ToList();
            }
        }

        public class HtSine
        {
            public List<double> Sine { get; set; }
            public List<double> LeadSine { get; set; }

            public HtSine(int recent, int early, double[] inReal)
            {
                Sine = new List<double>();
                LeadSine = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                double[] Returned1 = new double[early - recent];
                TicTacTec.TA.Library.Core.HtSine(recent, early, inReal, out begidx, out NBElement, Returned, Returned1);
                Sine = Returned.ToList();
                LeadSine = Returned1.ToList();
            }
        }

        public class HtTrendline
        {
            public List<double> HtTrendlineHist { get; set; }

            public HtTrendline(int recent, int early, double[] inReal)
            {
                HtTrendlineHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.HtTrendline(recent, early, inReal, out begidx, out NBElement, Returned);
                HtTrendlineHist = Returned.ToList();
            }
        }

        public class HtTrendMode
        {
            public List<int> HtTrendModeHist { get; set; }

            public HtTrendMode(int recent, int early, double[] inReal)
            {
                HtTrendModeHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.HtTrendMode(recent, early, inReal, out begidx, out NBElement, Returned);
                HtTrendModeHist = Returned.ToList();
            }
        }

        public class Kama
        {
            public List<double> KamaHist { get; set; }

            public Kama(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                KamaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Kama(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                KamaHist = Returned.ToList();
            }
        }

        public class LinearReg
        {
            public List<double> LinearRegHist { get; set; }

            public LinearReg(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                LinearRegHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.LinearReg(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                LinearRegHist = Returned.ToList();
            }
        }

        public class LinearRegAngle
        {
            public List<double> LinearRegAngleHist { get; set; }

            public LinearRegAngle(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                LinearRegAngleHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.LinearRegAngle(recent, early, inReal, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                LinearRegAngleHist = Returned.ToList();
            }
        }

        public class LinearRegIntercept
        {
            public List<double> LinearRegInterceptHist { get; set; }

            public LinearRegIntercept(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                LinearRegInterceptHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.LinearRegIntercept(recent, early, inReal, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                LinearRegInterceptHist = Returned.ToList();
            }
        }

        public class LinearRegSlope
        {
            public List<double> LinearRegSlopeHist { get; set; }

            public LinearRegSlope(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                LinearRegSlopeHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.LinearRegSlope(recent, early, inReal, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                LinearRegSlopeHist = Returned.ToList();
            }
        }

        public class Ln
        {
            public List<double> LnHist { get; set; }

            public Ln(int recent, int early, double[] inReal)
            {
                LnHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Ln(recent, early, inReal, out begidx, out NBElement, Returned);
                LnHist = Returned.ToList();
            }
        }

        public class Log10
        {
            public List<double> Log10Hist { get; set; }

            public Log10(int recent, int early, double[] inReal)
            {
                Log10Hist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Log10(recent, early, inReal, out begidx, out NBElement, Returned);
                Log10Hist = Returned.ToList();
            }
        }

        public class MacdExt
        {
            public List<double> MACD { get; set; }
            public List<double> MACDSignal { get; set; }
            public List<double> MACDHist { get; set; }

            public MacdExt(int recent, int early, double[] inReal, int optInFastPeriod, Core.MAType FastMAType,
                int optInSlowPeriod, Core.MAType SlowMAType, int optInSignalPeriod, Core.MAType SignalMAType)
            {
                MACD = new List<double>();
                MACDHist = new List<double>();
                MACDSignal = new List<double>();
                int begidx;
                int NBElement;
                double[] Macd = new double[early - recent];
                double[] MacdSignal = new double[early - recent];
                double[] MacdGist = new double[early - recent];
                TicTacTec.TA.Library.Core.MacdExt(recent, early, inReal, optInFastPeriod, FastMAType, optInSlowPeriod,
                    SlowMAType, optInSignalPeriod, SignalMAType, out begidx, out NBElement, Macd, MacdSignal, MacdGist);
                MACD = Macd.ToList();
                MACDSignal = MacdSignal.ToList();
                MACDHist = MacdGist.ToList();
            }
        }

        public class MacdFix
        {
            public List<double> MACD { get; set; }
            public List<double> MACDSignal { get; set; }
            public List<double> MACDHist { get; set; }

            public MacdFix(int recent, int early, double[] inReal, int optInSignalPeriod)
            {
                MACD = new List<double>();
                MACDSignal = new List<double>();
                MACDHist = new List<double>();
                int begidx;
                int NBElement;
                double[] macd = new double[early - recent];
                double[] macdsignal = new double[early - recent];
                double[] macdhist = new double[early - recent];
                TicTacTec.TA.Library.Core.MacdFix(recent, early, inReal, optInSignalPeriod, out begidx, out NBElement,
                    macd, macdsignal, macdhist);
                MACD = macd.ToList();
                MACDSignal = macdsignal.ToList();
                MACDHist = macdhist.ToList();
            }
        }

        public class Mama
        {
            public List<double> MAMA { get; set; }
            public List<double> FAMA { get; set; }

            public Mama(int recent, int early, double[] inReal, int optInFastLimit, int optInSlowLimit)
            {
                MAMA = new List<double>();
                FAMA = new List<double>();

                int begidx;
                int NBElement;
                double[] mama = new double[early - recent];
                double[] fama = new double[early - recent];
                TicTacTec.TA.Library.Core.Mama(recent, early, inReal, optInFastLimit, optInSlowLimit, out begidx,
                    out NBElement, mama, fama);
                MAMA = mama.ToList();
                FAMA = fama.ToList();
            }
        }

        public class Max
        {
            public List<double> MaxHist { get; set; }

            public Max(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MaxHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Max(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MaxHist = Returned.ToList();
            }
        }

        public class MaxIndex
        {
            public List<int> MaxIndexHist { get; set; }

            public MaxIndex(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MaxIndexHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.MaxIndex(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MaxIndexHist = Returned.ToList();
            }
        }

        public class MedPrice
        {
            public List<double> MedPriceHist { get; set; }

            public MedPrice(int recent, int early, double[] inHigh, double[] inLow)
            {
                MedPriceHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MedPrice(recent, early, inHigh, inLow, out begidx, out NBElement, Returned);
                MedPriceHist = Returned.ToList();
            }
        }

        public class Mfi
        {
            public List<double> MfiHist { get; set; }

            public Mfi(int recent, int early, double[] inHigh, double[] inLow, double[] inClose, double[] inVolume,
                int optInTimePeriod)
            {
                MfiHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Mfi(recent, early, inHigh, inLow, inClose, inVolume, optInTimePeriod,
                    out begidx, out NBElement, Returned);
                MfiHist = Returned.ToList();
            }
        }

        public class MidPoint
        {
            public List<double> MidPointHist { get; set; }

            public MidPoint(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MidPointHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MidPoint(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MidPointHist = Returned.ToList();
            }
        }

        public class MidPrice
        {
            public List<double> MidPriceHidt { get; set; }

            public MidPrice(int recent, int early, double[] inHigh, double[] inLow, int optInTimePeriod)
            {
                MidPriceHidt = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MidPrice(recent, early, inHigh, inLow, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                MidPriceHidt = Returned.ToList();
            }
        }

        public class Min
        {
            public List<double> MinHist { get; set; }

            public Min(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MinHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Min(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MinHist = Returned.ToList();
            }
        }

        public class MinIndex
        {
            public List<int> MinIndexHist { get; set; }

            public MinIndex(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MinIndexHist = new List<int>();
                int begidx;
                int NBElement;
                int[] Returned = new int[early - recent];
                TicTacTec.TA.Library.Core.MinIndex(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MinIndexHist = Returned.ToList();
            }
        }

        public class MinMax
        {
            public List<double> MinHist { get; set; }
            public List<double> MaxHist { get; set; }

            public MinMax(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MinHist = new List<double>();
                MaxHist = new List<double>();

                int begidx;
                int NBElement;
                double[] outMin = new double[early - recent];
                double[] outmax = new double[early - recent];

                TicTacTec.TA.Library.Core.MinMax(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    outMin, outmax);
                MinHist = outMin.ToList();
                MaxHist = outmax.ToList();
            }
        }

        public class MinusDI
        {
            public List<double> MinusDIHist { get; set; }

            public MinusDI(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose,
                int optInTimePeriod)
            {
                MinusDIHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MinusDI(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                MinusDIHist = Returned.ToList();
            }
        }

        public class MinusDM
        {
            public List<double> MinusDMHist { get; set; }

            public MinusDM(int recent, int early, double[] inOpen, double[] inHigh, double[] inLow, double[] inClose,
                int optInTimePeriod)
            {
                MinusDMHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MinusDM(recent, early, inHigh, inLow, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                MinusDMHist = Returned.ToList();
            }
        }

        public class Mom
        {
            public List<double> MomHist { get; set; }

            public Mom(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                MomHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Mom(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                MomHist = Returned.ToList();
            }
        }

        public class MovingAverage
        {
            public List<double> MovingAverageHist { get; set; }

            public MovingAverage(int recent, int early, double[] inReal, int optInTimePeriod, Core.MAType MAType)
            {
                MovingAverageHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MovingAverage(recent, early, inReal, optInTimePeriod, MAType, out begidx,
                    out NBElement, Returned);
                MovingAverageHist = Returned.ToList();
            }
        }

        public class MovingAverageVariablePeriod
        {
            public List<double> MovingAverageVariablePeriodHist { get; set; }

            public MovingAverageVariablePeriod(int recent, int early, double[] inReal, double[] inPeriods,
                int optInMinPeriod, int optInMaxPeriod, Core.MAType MAType)
            {
                MovingAverageVariablePeriodHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.MovingAverageVariablePeriod(recent, early, inReal, inPeriods, optInMinPeriod,
                    optInMaxPeriod, MAType, out begidx, out NBElement, Returned);
                MovingAverageVariablePeriodHist = Returned.ToList();
            }
        }

        public class Mult
        {
            public List<double> MultHist { get; set; }

            public Mult(int recent, int early, double[] inReal0, double[] inReal1)
            {
                MultHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Mult(recent, early, inReal0, inReal1, out begidx, out NBElement, Returned);
                MultHist = Returned.ToList();
            }
        }

        public class Natr
        {
            public List<double> NatrHist { get; set; }

            public Natr(int recent, int early, int optInTimePeriod, double[] inHigh, double[] inLow, double[] inClose)
            {
                NatrHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Natr(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                NatrHist = Returned.ToList();
            }
        }

        public class Obv
        {
            public List<double> ObvHist { get; set; }

            public Obv(int recent, int early, double[] inReal, double[] inVolume)
            {
                ObvHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Obv(recent, early, inReal, inVolume, out begidx, out NBElement, Returned);
                ObvHist = Returned.ToList();
            }
        }

        public class PlusDI
        {
            public List<double> PlusDIHist { get; set; }

            public PlusDI(int recent, int early, int optInTimePeriod, double[] inHigh, double[] inLow, double[] inClose)
            {
                PlusDIHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.PlusDI(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                PlusDIHist = Returned.ToList();
            }
        }

        public class PlusDM
        {
            public List<double> PlusDMHist { get; set; }

            public PlusDM(int recent, int early, int optInTimePeriod, double[] inHigh, double[] inLow)
            {
                PlusDMHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.PlusDM(recent, early, inHigh, inLow, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                PlusDMHist = Returned.ToList();
            }
        }

        public class Ppo
        {
            public List<double> PpoHist { get; set; }

            public Ppo(int recent, int early, double[] inReal, int optInFastPeriod, int optInSlowPeriod,
                Core.MAType optInMAType)
            {
                PpoHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Ppo(recent, early, inReal, optInFastPeriod, optInSlowPeriod, optInMAType,
                    out begidx, out NBElement, Returned);
                PpoHist = Returned.ToList();
            }
        }

        public class Roc
        {
            public List<double> RocHist { get; set; }

            public Roc(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                RocHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Roc(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                RocHist = Returned.ToList();
            }
        }

        public class RocP
        {
            public List<double> RocPHist { get; set; }

            public RocP(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                RocPHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.RocP(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                RocPHist = Returned.ToList();
            }
        }

        public class RocR
        {
            public List<double> RocRHist { get; set; }

            public RocR(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                RocRHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.RocR(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                RocRHist = Returned.ToList();
            }
        }

        public class RocR100
        {
            public List<double> RocR100Hist { get; set; }

            public RocR100(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                RocR100Hist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.RocR100(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                RocR100Hist = Returned.ToList();
            }
        }

        public class Sar
        {
            public List<double> SarHist { get; set; }

            public Sar(int recent, int early, double optInAcceleration, double[] inHigh, double[] inLow,
                double optInMaximum)
            {
                SarHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sar(recent, early, inHigh, inLow, optInAcceleration, optInMaximum, out begidx,
                    out NBElement, Returned);
                SarHist = Returned.ToList();
            }
        }

        public class SarExt
        {
            public List<double> SarExtHist { get; set; }

            public SarExt(int recent, int early, double[] inHigh, double[] inLow, double optInStartValue,
                double optInOffsetOnReverse, double optInAccelerationInitLong, double optInAccelerationLong,
                double optInAccelerationMaxLong, double optInAccelerationInitShort, double optInAccelerationShort,
                double optInAccelerationMaxShort)
            {
                SarExtHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.SarExt(recent, early, inHigh, inLow, optInStartValue, optInOffsetOnReverse,
                    optInAccelerationInitLong, optInAccelerationLong, optInAccelerationMaxLong,
                    optInAccelerationInitShort, optInAccelerationShort, optInAccelerationMaxShort, out begidx,
                    out NBElement, Returned);
                SarExtHist = Returned.ToList();
            }
        }

        public class Sin
        {
            public List<double> SinHist { get; set; }

            public Sin(int recent, int early, double[] inReal)
            {
                SinHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sin(recent, early, inReal, out begidx, out NBElement, Returned);
                SinHist = Returned.ToList();
            }
        }

        public class Sinh
        {
            public List<double> SinhHist { get; set; }

            public Sinh(int recent, int early, double[] inReal)
            {
                SinhHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sinh(recent, early, inReal, out begidx, out NBElement, Returned);
                SinhHist = Returned.ToList();
            }
        }

        public class Sma
        {
            public List<double> SmaHist { get; set; }

            public Sma(int recent, int early, double[] inReal, int optInTimePeriod, double[] inLow, double[] inClose)
            {
                SmaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sma(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                SmaHist = Returned.ToList();
            }
        }

        public class Sqrt
        {
            public List<double> SqrtHist { get; set; }

            public Sqrt(int recent, int early, double[] inReal)
            {
                SqrtHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sqrt(recent, early, inReal, out begidx, out NBElement, Returned);
                SqrtHist = Returned.ToList();
            }
        }

        public class StdDev
        {
            public List<double> StdDevHist { get; set; }

            public StdDev(int recent, int early, double[] inReal, int optInTimePeriod, int optInNbDev, double[] inClose)
            {
                StdDevHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.StdDev(recent, early, inReal, optInTimePeriod, optInNbDev, out begidx,
                    out NBElement, Returned);
                StdDevHist = Returned.ToList();
            }
        }

        public class StochF
        {
            public List<double> FastKHist { get; set; }
            public List<double> FastDHist { get; set; }


            public StochF(int recent, int early, double[] inHigh, double[] inLow, double[] inClose,
                int optInFastK_Period, int optInFastD_Period, Core.MAType optInFastD_MAType)
            {
                FastKHist = new List<double>();
                FastDHist = new List<double>();

                int begidx;
                int NBElement;
                double[] FastK = new double[early - recent];
                double[] FastD = new double[early - recent];

                TicTacTec.TA.Library.Core.StochF(recent, early, inHigh, inLow, inClose, optInFastK_Period,
                    optInFastD_Period, optInFastD_MAType, out begidx, out NBElement, FastK, FastD);
                FastKHist = FastK.ToList();
                FastDHist = FastD.ToList();

            }
        }

        public class StochRsi
        {
            public List<double> FastKHist { get; set; }
            public List<double> FastDHist { get; set; }


            public StochRsi(int recent, int early, double[] inReal, int optInTimePeriod, int optInFastK_Period,
                int optInFastD_Period, Core.MAType optInFastD_MAType)
            {
                FastKHist = new List<double>();
                FastDHist = new List<double>();

                int begidx;
                int NBElement;
                double[] outFastK = new double[early - recent];
                double[] outFastD = new double[early - recent];

                TicTacTec.TA.Library.Core.StochRsi(recent, early, inReal, optInTimePeriod, optInFastK_Period,
                    optInFastD_Period, optInFastD_MAType, out begidx, out NBElement, outFastK, outFastD);
                FastKHist = outFastK.ToList();
                FastDHist = outFastD.ToList();

            }
        }

        public class Sub
        {
            public List<double> SubHist { get; set; }

            public Sub(int recent, int early, double[] inReal0, double[] inReal1)
            {
                SubHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sub(recent, early, inReal0, inReal1, out begidx, out NBElement, Returned);
                SubHist = Returned.ToList();
            }
        }

        public class Sum
        {
            public List<double> SumHist { get; set; }

            public Sum(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                SumHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Sum(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                SumHist = Returned.ToList();
            }
        }

        public class T3
        {
            public List<double> T3Hist { get; set; }

            public T3(int recent, int early, double[] inReal, int optInTimePeriod, int optInVFactor, double[] inClose)
            {
                T3Hist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.T3(recent, early, inReal, optInTimePeriod, optInVFactor, out begidx,
                    out NBElement, Returned);
                T3Hist = Returned.ToList();
            }
        }

        public class Tan
        {
            public List<double> TanHist { get; set; }

            public Tan(int recent, int early, double[] inReal)
            {
                TanHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Tan(recent, early, inReal, out begidx, out NBElement, Returned);
                TanHist = Returned.ToList();
            }
        }

        public class Tanh
        {
            public List<double> TanhHist { get; set; }

            public Tanh(int recent, int early, double[] inReal)
            {
                TanhHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Tanh(recent, early, inReal, out begidx, out NBElement, Returned);
                TanhHist = Returned.ToList();
            }
        }

        public class Tema
        {
            public List<double> TemaHist { get; set; }

            public Tema(int recent, int early, double[] inReal, int optInTimePeriod, double[] inLow, double[] inClose)
            {
                TemaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Tema(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                TemaHist = Returned.ToList();
            }
        }

        public class Trima
        {
            public List<double> TrimaHist { get; set; }

            public Trima(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                TrimaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Trima(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                TrimaHist = Returned.ToList();
            }
        }

        public class Trix
        {
            public List<double> TrixHist { get; set; }

            public Trix(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                TrixHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Trix(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                TrixHist = Returned.ToList();
            }
        }

        public class TrueRange
        {
            public List<double> TrueRangeHist { get; set; }

            public TrueRange(int recent, int early, double[] inHigh, double[] inLow, double[] inClose)
            {
                TrueRangeHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.TrueRange(recent, early, inHigh, inLow, inClose, out begidx, out NBElement,
                    Returned);
                TrueRangeHist = Returned.ToList();
            }
        }

        public class Tsf
        {
            public List<double> TsfHist { get; set; }

            public Tsf(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                TsfHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Tsf(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                TsfHist = Returned.ToList();
            }
        }

        public class TypPrice
        {
            public List<double> TypPriceHist { get; set; }

            public TypPrice(int recent, int early, double[] inHigh, double[] inLow, double[] inClose)
            {
                TypPriceHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.TypPrice(recent, early, inHigh, inLow, inClose, out begidx, out NBElement,
                    Returned);
                TypPriceHist = Returned.ToList();
            }
        }

        public class UltOsc
        {
            public List<double> UltOscHist { get; set; }

            public UltOsc(int recent, int early, double[] inHigh, double[] inLow, double[] inClose, int optInTimePeriod1,
                int optInTimePeriod2, int optInTimePeriod3)
            {
                UltOscHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.UltOsc(recent, early, inHigh, inLow, inClose, optInTimePeriod1,
                    optInTimePeriod2, optInTimePeriod3, out begidx, out NBElement, Returned);
                UltOscHist = Returned.ToList();
            }
        }

        public class Variance
        {
            public List<double> VarianceHist { get; set; }

            public Variance(int recent, int early, double[] inReal, int optInTimePeriod, double optInNbDev)
            {
                VarianceHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Variance(recent, early, inReal, optInTimePeriod, optInNbDev, out begidx,
                    out NBElement, Returned);
                VarianceHist = Returned.ToList();
            }
        }

        public class WclPrice
        {
            public List<double> WclPriceHist { get; set; }

            public WclPrice(int recent, int early, double[] inHigh, double[] inLow, double[] inClose)
            {
                WclPriceHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.WclPrice(recent, early, inHigh, inLow, inClose, out begidx, out NBElement,
                    Returned);
                WclPriceHist = Returned.ToList();
            }
        }

        public class WillR
        {
            public List<double> WillRHist { get; set; }

            public WillR(int recent, int early, int optInTimePeriod, double[] inHigh, double[] inLow, double[] inClose)
            {
                WillRHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.WillR(recent, early, inHigh, inLow, inClose, optInTimePeriod, out begidx,
                    out NBElement, Returned);
                WillRHist = Returned.ToList();
            }
        }

        public class Wma
        {
            public List<double> WmaHist { get; set; }

            public Wma(int recent, int early, double[] inReal, int optInTimePeriod)
            {
                WmaHist = new List<double>();
                int begidx;
                int NBElement;
                double[] Returned = new double[early - recent];
                TicTacTec.TA.Library.Core.Wma(recent, early, inReal, optInTimePeriod, out begidx, out NBElement,
                    Returned);
                WmaHist = Returned.ToList();
            }
        }
    }
}
 


